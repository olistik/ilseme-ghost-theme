{{!< default}}
{{#post}}
<article id="{{id}}" class="{{post_class}}">
	<span class="post-stamp">{{date published_at format='MMMM Do, YYYY'}}</span>
	<h1 class="post-title">{{title}}</h1>
	{{{content}}}
</article>
{{/post}}
